# 09L-11_task_011_checklist

> **Task ID**: T011
> **Task 명**: E2E 통합 테스트 및 최적화
> **관련 문서**: 07B-02 (Sec 8)
> **예상 소요**: 2 hours

---

## Step 1: 목표 이해 ✅

### 1.1 Task 목표

- 프로덕션 빌드 생성 및 검증.
- 마이크 권한 거부 등 예외 상황 처리.
- 콘솔 에러 제거.

---

## Step 2: 테스트 작성 (Manual Scenario) 🧪

*E2E는 자동화보다 시나리오 기반 수동 검증이 효율적일 수 있습니다.*

**시나리오**:

1. 마이크 권한 거부 시 -> "권한 필요" 알림 표시.
2. 서버 연결 실패 시 -> "연결 오류" 배지 표시.
3. 30분 이상 장시간 사용 시 메모리 누수 확인.

---

## Step 3: 구현 (Error Handling) 🔨

### 3.1 에러 UI

- `src/layouts/MainLayout.tsx`에 `GlobalErrorToast` 추가.
- Store의 `connectionStatus === 'error'`일 때 붉은색 배지 표시.

### 3.2 빌드 설정

- `vite.config.ts`: `console.log` 제거 플러그인(선택) 또는 수동 제거.

---

## Step 4: 정적 검증 🔍

- [ ] `npm run build` 실행 시 에러 없음.
- [ ] `dist/` 폴더 생성 확인.

---

## Step 5: 테스트 실행 ✅

- [ ] `npm run preview`로 빌드된 결과물 로컬 서빙 및 동작 확인.

---

## Step 6: 리팩토링 ✨

- [ ] 미사용 Import 및 변수 제거 (`eslint` 경고 확인).

---

## Step 7: 통합 확인 🔗

- 모든 기능이 유기적으로 동작하는지 최종 리허설.

---

## Step 8: 문서화 📝

- `README.md`에 배포 방법(Deployment) 섹션 추가.

---

## Step 9: 커밋 ✅

```bash
git add .
git commit -m "chore(release): Finalize build setup and error handling
```

